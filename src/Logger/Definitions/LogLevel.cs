namespace RabbitMQLogger.Definitions
{
    public enum LogLevel
    {
        NotSet = 0,
        Debug = 10,
        Info = 20,
        Notice = 30,
        Warning = 40,
        Error = 50,
        Critical = 60,
        Alert = 70,
        Emergency = 80
    }
}
